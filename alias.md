# Alias to setup


```bash
alias k=kubectl
alias kg='kubectl get'
alias kd='kubectl delete'
alias ka='k apply -f '
alias ke='k exec -it '
export kn='kubectl config set-context --current --namespace '
export d='--dry-run=client -o yaml'
```
